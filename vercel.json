{
  "installCommand": "npm install",
  "buildCommand": "npx vite build",
  "outputDirectory": "dist/client",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
